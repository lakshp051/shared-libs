library identifier: "com-lp-jenkin@master",
        retriever: modernSCM(
            [
                $class: 'GitSCMSource',
                remote: "https://github.com/lakshp051/shared_libs.git"
            ]
        )


pipeline {
    
    agent {
        label 'local'
    }
    
    stages {
        stage('Build') {
            steps {
                script {
                    println(globalVars.msg1)
                    println(globalVars.msg2)
                    printMessage.display()
                }
            }
        }
    }
}
