library identifier: "com-lp-jenkin@master",
        retriever: modernSCM(
            [
                $class: 'GitSCMSource',
                remote: "https://github.com/lakshp051/shared_libs.git"
            ]
        )


pipeline {
    
    agent {
        label 'local'
    }
    
    stages {
        stage('Build') {
            steps {
                script {
                    utils = load 'src/constants.groovy'
                    println(msg1)
                    println(msg2)
                    printMessage.display()
                }
            }
        }
    }
}
